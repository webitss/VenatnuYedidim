import { EditAndDeletePipe } from './edit-and-delete.pipe';

describe('EditAndDeletePipe', () => {
  it('create an instance', () => {
    const pipe = new EditAndDeletePipe();
    expect(pipe).toBeTruthy();
  });
});
