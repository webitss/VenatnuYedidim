import { FilterBMultySelectCheckedPipe } from './filter-b-multy-select-checked.pipe';

describe('FilterBMultySelectCheckedPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBMultySelectCheckedPipe();
    expect(pipe).toBeTruthy();
  });
});
